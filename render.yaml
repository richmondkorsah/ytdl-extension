services:
  - type: web
    name: yt-downloader
    runtime: python
    buildCommand: "apt-get update -qq && apt-get install -y ffmpeg && curl -fsSL https://deno.land/install.sh | sh && pip install -r requirements.txt"
    startCommand: "python backend/server.py"
    envVars:
      - key: FLASK_DEBUG
        value: "false"
